# AWS Region
aws_region = "us-east-1"

# EC2 instance type (high enough for Docker + Jenkins, not too costly)
instance_type = "t3.large"   # 2 vCPU, 8 GB RAM

# Existing AWS Key Pair name (must exist in your AWS account)
key_name = "Task1"
