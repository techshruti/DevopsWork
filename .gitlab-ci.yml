image:
  name: hashicorp/terraform:1.5.7
  entrypoint: ["/bin/sh", "-c"]

stages:
  - init
  - validate

terraform_init:
  stage: init
  script:
    - terraform init -backend=false

terraform_validate:
  stage: validate
  script:
    - terraform validate
